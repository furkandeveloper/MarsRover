using System;
using System.Collections.Generic;
using System.Text;

namespace MarsRover.Models
{
    public class Enums
    {
        public enum Direction
        {
             N = 0,
             S = 1,
             E = 2,
             W = 3
        }

        public enum Move
        {
            L = 0,
            R = 1,
            M = 2
        }
    }
}
